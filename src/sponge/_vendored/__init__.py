"""Vendored copies of sibling packages, pinned to their state at extraction time.

These are bundled for self-contained installs. When the upstream packages
publish to PyPI, swap the imports in `sponge/__init__.py` and remove this dir.
"""
